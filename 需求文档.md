# 大众点评小程序自动化采集系统 - 详细设计文档

## Context
基于Python+subprocess+OpenCV 技术栈，设计一个自动化工具，，实现自动化操作。

**业务目标**：自动遍历大众点评小程序的所有区域，采集每个区域下所有商户的详细信息。

---

## 一、业务流程设计

### 1.1 完整业务流程

```
开始
  ↓
[1] 在点评小程序首页，识别并点击"美食"按钮
  ↓
[2] 进入美食列表页
  ↓
[3] 点击筛选项：距离、区域、评分、新店
  ↓
[4] 开始数据采集循环(已经实现了rust版本)
  ↓
  ┌─────────────────────────────────┐
  │ [4.1] 抓取当前页面商户数据      │
  │ [4.2] 向下滚动加载更多          │
  │ [4.3] 检测是否到达底部          │
  │       ("没有数据了")            │
  └─────────────────────────────────┘
  ↓
  是否到达底部？
  ├─ 否 → 返回 [4.1]
  └─ 是 ↓
  ↓
[5] 切换到下一个区域
  ↓
  是否所有区域已遍历？
  ├─ 否 → 返回 [4]
  └─ 是 ↓
  ↓
[6] 数据采集完成，导出结果
  ↓
结束
```

### 1.2 详细步骤拆解

#### 步骤 1：点击"美食"按钮

**目标**：从点评小程序首页进入美食分类

**操作流程**：
1. 截取当前屏幕
2. 使用模板匹配识别"美食"按钮位置
3. 点击按钮（添加随机偏移）
4. 等待页面跳转（500-1000ms）
5. 验证是否成功进入列表页

**识别策略**：
- 主模板：`food_icon.png`（美食图标）
- 备用策略：OCR 识别"美食"文字
- 匹配阈值：0.85
- ROI 区域：屏幕上半部分（导航区域）

**异常处理**：
- 识别失败：降低阈值到 0.75，重试 3 次
- 点击无效：检测页面是否变化，未变化则重新点击
- 超时：返回首页重新开始

---

#### 步骤 2：进入列表页验证

**目标**：确认已成功进入美食列表页

**验证方法**：
1. 检测筛选按钮是否出现（距离、区域、评分、新店）
2. 检测列表卡片元素
3. 检测页面 URL 变化（通过抓包数据）

**识别策略**：
- 模板：`filter_bar.png`（筛选栏）
- 模板：`merchant_card.png`（商户卡片）
- 超时时间：5 秒

**异常处理**：
- 验证失败：返回首页重新开始
- 出现弹窗：识别并关闭弹窗

---

#### 步骤 3：点击筛选项

**目标**：依次点击好评优先、区域、评分、新店等筛选条件

**操作流程**：
```
3.1 点击"距离"筛选
    ├─ 识别"距离"按钮
    ├─ 点击展开下拉菜单
    ├─ 选择默认选项（或指定距离）
    └─ 等待列表刷新

3.2 点击"区域"筛选
    ├─ 识别"区域"按钮
    ├─ 点击展开区域列表
    ├─ 记录所有可选区域（用于后续遍历）
    ├─ 选择第一个区域
    └─ 等待列表刷新

3.3 点击"评分"筛选
    ├─ 识别"评分"按钮
    ├─ 点击展开评分选项
    ├─ 选择指定评分（如 4.5 分以上）
    └─ 等待列表刷新

3.4 点击"新店"筛选（可选）
    ├─ 识别"新店"按钮
    ├─ 点击切换状态
    └─ 等待列表刷新
```


 核心功能

  1. GUI节点编辑器 - 可视化配置操作步骤
  2. 模板匹配 - 基于OpenCV的多尺度识别
  3. 坐标缓存 - 提升识别速度
  4. 人类化操作 - 随机偏移、贝塞尔曲线、随机延迟
  5. 区域遍历 - 自动遍历所有区域
  6. 滚动采集 - 自动滚动并识别"加载完了"
  7. Rust集成 - 与抓包工具无缝集成

  📝 下一步操作

  # 1. 安装依赖
  pip install -r requirements.txt

  # 2. 测试系统
  python test_system.py

  # 3. 配置节点（GUI）
  python node_editor_app.py

  # 4. 运行自动化
  python main.py

  📚 文档说明

  - README.md - 完整的使用文档和API说明
  - QUICKSTART.md - 快速开始指南，包含详细步骤
  - PROJECT_SUMMARY.md - 项目实现总结
  - 需求文档.md - 原始业务需求

  🔧 关键文件

  - main.py - 自动化执行主入口
  - node_editor_app.py - GUI编辑器入口
  - test_system.py - 系统测试脚本
  - config/nodes.json - 节点配置
  - config/regions.json - 区域列表

  系统已完全就绪，可以开始使用了！🚀
