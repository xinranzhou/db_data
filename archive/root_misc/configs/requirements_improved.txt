# 核心依赖 - 跨平台
pillow>=9.0.0

# GUI框架 - 跨平台
PyQt5>=5.15.0

# 配置和日志 - 跨平台
pyyaml>=6.0
loguru>=0.6.0

# 重试机制 - 跨平台
tenacity>=8.0.0

# 环境变量 - 跨平台
python-dotenv>=0.19.0

# 抓包与数据导出 - 跨平台
mitmproxy>=10.1.0
qrcode[pil]>=7.4.2
openpyxl>=3.1.0
playwright>=1.52.0

# 平台特定依赖
# macOS特定 - 仅在macOS上安装
pyobjc-framework-Quartz>=8.0; sys_platform == 'darwin'

# Windows特定 - 仅在Windows上安装
# pywin32>=300; sys_platform == 'win32'

# Linux特定 - 仅在Linux上安装
# python-xlib>=0.33; sys_platform == 'linux'

# 打包工具（开发依赖）
pyinstaller>=5.0; extra == 'dev'
